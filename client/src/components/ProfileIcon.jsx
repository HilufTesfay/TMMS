import { BsPersonCircle } from "react-icons/bs";

const ProfileIcon = () => {
  return (
    <BsPersonCircle size={40} className="text-gray-500 hover:cursor-pointer" />
  );
};

export default ProfileIcon;
